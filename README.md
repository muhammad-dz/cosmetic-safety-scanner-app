# Cosmetic Safety Scanner App

A web application for scanning cosmetic products and analyzing safety.

## Setup
1. Backend: `cd backend && pip install -r requirements.txt`
2. Frontend: `cd frontend && npm install`
